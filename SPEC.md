# TIFF Scheduler 仕様書

## 1. 概要

TIFF Scheduler は、東京国際映画祭（TIFF）で鑑賞したい映画を選択すると、上映日時・上映会場・会場間の移動時間・昼食時間・勤務日を考慮し、現実的な鑑賞スケジュールを自動生成する Web アプリケーションである。

本アプリの最優先目的は、選択した映画を可能な限りすべて鑑賞しつつ、映画祭参加のために取得する休暇日数を最小化することである。

初期バージョンは React + TypeScript + Vite による完全静的 SPA とし、GitHub Pages でホスティングする。バックエンドおよびデータベースは使用しない。

---

## 2. 目的

ユーザーが「見たい映画」を選択するだけで、以下を満たす鑑賞計画を提示する。

- 選択した映画を可能な限りすべて鑑賞できる
- 同一作品に複数上映回がある場合、最適な上映回を自動選択する
- 上映時間の重複を避ける
- 映画館間の現実的な移動時間を確保する
- 上映終了後の退場時間、次上映への入場余裕時間を確保する
- 昼食時間を確保する
- 平日に必要となる休暇取得日数を最小化する
- 同じ休暇日数なら、移動時間や待ち時間が少ないプランを優先する
- 全作品を鑑賞できない場合、その理由と代替案を提示する

---

## 3. 対象環境

### 3.1 フロントエンド

- React
- TypeScript
- Vite

### 3.2 ホスティング

- GitHub Pages
- GitHub Actions から自動デプロイ

### 3.3 バックエンド

MVP では使用しない。

スケジュール計算はすべてブラウザ内で実行する。

---

## 4. 主要ユースケース

### UC-01 見たい映画を選択する

ユーザーは TIFF の上映作品一覧から鑑賞したい映画を複数選択できる。

### UC-02 スケジュールを生成する

選択した映画に対し、条件を満たす上映回の組み合わせを探索し、おすすめプランを生成する。

### UC-03 複数プランを比較する

条件を満たすプランが複数存在する場合、上位候補を表示する。

MVP では最大 3 件を表示する。

### UC-04 鑑賞不可能な組み合わせを確認する

選択した全作品を鑑賞できない場合、競合する作品または上映回を示し、最大限鑑賞可能な代替プランを提示する。

### UC-05 勤務条件を設定する

ユーザーは通常勤務曜日、追加休日、休暇を取得できない日を設定できる。

---

## 5. 画面仕様

## 5.1 作品選択画面

表示項目：

- 作品名
- 上映日
- 上映回数
- 上映時間
- 上映会場
- 選択チェックボックス

操作：

- 作品の選択・解除
- スケジュール生成
- 条件設定画面の表示

スマートフォンでの利用を主なユースケースの一つとし、レスポンシブ対応する。

---

## 5.2 条件設定

### 勤務曜日

初期値：

- 月曜日: 勤務
- 火曜日: 勤務
- 水曜日: 勤務
- 木曜日: 勤務
- 金曜日: 勤務
- 土曜日: 休日
- 日曜日: 休日

### 追加休日

ユーザーが任意の日付を「もともと休み」として登録できる。

この日は休暇日数として数えない。

### 休暇取得不可日

ユーザーが任意の日付を「休めない日」として登録できる。

この日に勤務時間と重なる上映を含むプランは生成しない。

### 昼食設定

初期値：

- 昼食可能時間帯: 11:30〜14:30
- 最低昼食時間: 45 分

将来的に 30 / 45 / 60 分から選択可能にする。

### 移動バッファ

初期値：

- 上映終了後の退場バッファ: 5 分
- 次上映の入場バッファ: 10 分

---

## 5.3 スケジュール結果画面

プランごとに以下を表示する。

- 必要休暇日数
- 鑑賞作品数
- 総移動時間
- 総待ち時間
- 日別タイムライン

タイムライン例：

```text
10/30（金） 有給

10:00 - 12:00
映画 A
TOHOシネマズ日比谷

12:00 - 12:10
徒歩移動

12:15 - 13:00
昼食

13:30 - 15:40
映画 B
TOHOシネマズ シャンテ
```

---

## 6. データモデル

## 6.1 Film

```ts
interface Film {
  id: string;
  title: string;
  originalTitle?: string;
  durationMinutes: number;
  url?: string;
}
```

## 6.2 Screening

```ts
interface Screening {
  id: string;
  filmId: string;
  venueId: string;
  startAt: string;
  endAt: string;
  eventBeforeMinutes?: number;
  eventAfterMinutes?: number;
}
```

`eventBeforeMinutes` / `eventAfterMinutes` は、舞台挨拶や Q&A など、本編上映以外に必要な時間を表す。

## 6.3 Venue

```ts
interface Venue {
  id: string;
  name: string;
  address?: string;
}
```

## 6.4 TravelTime

```ts
interface TravelTime {
  fromVenueId: string;
  toVenueId: string;
  minutes: number;
}
```

会場間移動時間はブラウザ実行時に外部地図 API を呼ばず、事前作成した移動時間マトリクスを使用する。

## 6.5 UserConstraints

```ts
interface UserConstraints {
  workingWeekdays: number[];
  additionalDaysOff: string[];
  unavailableDates: string[];
  lunchWindowStart: string;
  lunchWindowEnd: string;
  lunchDurationMinutes: number;
  exitBufferMinutes: number;
  arrivalBufferMinutes: number;
}
```

---

## 7. 静的データ構成

```text
src/data/
├── films.json
├── screenings.json
├── venues.json
└── travel-times.json
```

MVP では TIFF の上映情報を JSON としてリポジトリ内に保持する。

ブラウザから TIFF 公式サイトへ直接アクセスしない。

---

## 8. スケジュール制約

## 8.1 上映時間重複

同時刻に重なる上映は同一プランに含めない。

---

## 8.2 会場移動

上映 A の後に上映 B を鑑賞するには、以下を満たす必要がある。

```text
B.start >=
  A.end
  + A.eventAfterMinutes
  + exitBufferMinutes
  + travelTime(A.venue, B.venue)
  + arrivalBufferMinutes
```

同一会場の場合も最低限の移動・入場バッファを考慮する。

---

## 8.3 昼食

その日に一定時間以上映画を鑑賞する場合、指定した昼食可能時間帯の中に最低昼食時間以上の連続した空き時間が存在することを必須とする。

初期値：

```text
11:30〜14:30 の間に 45 分以上
```

MVP 実装時には「どの日を昼食制約の対象にするか」を明示的に定義する。

候補：

- 午前・午後をまたいで鑑賞する日のみ必須
- 11:30〜14:30 の間に上映が存在する日のみ必須

---

## 8.4 休暇日

通常勤務日に勤務時間と重なる上映を鑑賞する場合、その日は休暇 1 日として数える。

以下は休暇日数に含めない。

- 土日など通常休日
- 祝日
- ユーザーが追加休日として設定した日

MVP では日本の祝日データを静的データとして保持するか、ビルド時に生成する。

---

## 8.5 休暇取得不可日

ユーザーが指定した休暇取得不可日に、勤務時間と重なる上映があるプランは無効とする。

---

## 9. 最適化方針

最適化は加重スコアではなく、原則として辞書式比較を使用する。

優先順位：

1. 選択した作品の鑑賞数を最大化する
2. 必要休暇日数を最小化する
3. 総移動時間を最小化する
4. 総待ち時間を最小化する
5. 必要に応じて遅い時間帯の上映を避ける

全選択作品を鑑賞可能な場合は、鑑賞数が同一なので休暇日数最小化が実質的な最優先条件となる。

比較値の例：

```ts
interface ScheduleScore {
  missedFilmCount: number;
  vacationDays: number;
  travelMinutes: number;
  waitingMinutes: number;
}
```

比較順：

```text
missedFilmCount
→ vacationDays
→ travelMinutes
→ waitingMinutes
```

すべて昇順で評価する。

---

## 10. 探索アルゴリズム

MVP では専用の数理最適化ライブラリを使用せず、TypeScript 上で以下を実装する。

- DFS（深さ優先探索）
- Branch and Bound
- 早期枝刈り

### 探索単位

1 作品につき 1 上映回を選択する。

### 探索順

上映候補が少ない作品から探索する。

例：

```text
映画 A: 1 回
映画 B: 4 回
映画 C: 2 回

探索順
A → C → B
```

これにより無効な組み合わせを早期に発見し、探索空間を縮小する。

### 枝刈り条件

例：

- 既存上映との時間衝突
- 会場間移動が間に合わない
- 休暇取得不可日に該当する
- 昼食制約を満たせないことが確定した
- 現時点ですでに既知の最適解より休暇日数が多い
- 残り作品をすべて鑑賞しても既知解の鑑賞数を上回れない

---

## 11. 全作品を鑑賞できない場合

単に「スケジュールを作成できません」と表示しない。

以下を提示する。

- 最大鑑賞可能作品数
- 鑑賞できない作品
- 主な競合上映
- どの作品を外せば成立するか
- 最大鑑賞数となる代替プラン

例：

```text
選択した 10 作品すべてを鑑賞できる組み合わせはありません。

最大 9 作品を鑑賞できます。

映画 A と映画 B の上映候補が競合しています。
映画 B を外すと、残り 9 作品を休暇 1 日で鑑賞できます。
```

---

## 12. ディレクトリ構成案

```text
TIFFScheduler/
├── .github/
│   └── workflows/
│       └── deploy.yml
├── src/
│   ├── components/
│   ├── pages/
│   ├── scheduler/
│   │   ├── optimizer.ts
│   │   ├── constraints.ts
│   │   ├── scoring.ts
│   │   └── types.ts
│   ├── data/
│   │   ├── films.json
│   │   ├── screenings.json
│   │   ├── venues.json
│   │   └── travel-times.json
│   ├── App.tsx
│   └── main.tsx
├── scripts/
│   └── import-tiff.ts
├── index.html
├── package.json
├── tsconfig.json
├── vite.config.ts
└── SPEC.md
```

---

## 13. TIFF データ取り込み

MVP では、上映データは JSON に変換してリポジトリへ保存する。

将来的には `scripts/import-tiff.ts` を実装し、TIFF 公式サイトから上映情報を取得して JSON を生成する。

想定フロー：

```text
TIFF 公式サイト
      ↓
scripts/import-tiff.ts
      ↓
films.json / screenings.json
      ↓
Git commit
      ↓
GitHub Pages 再デプロイ
```

将来的には GitHub Actions から定期実行し、上映情報に変更があった場合のみデータ更新・再デプロイすることも検討する。

取得処理は TIFF 公式サイトの利用条件および robots.txt 等を確認した上で実装する。

---

## 14. GitHub Pages デプロイ

`main` ブランチへの push をトリガーとして GitHub Actions を実行する。

想定フロー：

```text
push main
   ↓
npm ci
   ↓
npm run build
   ↓
dist/
   ↓
GitHub Pages
```

Vite の `base` は GitHub Pages のリポジトリ名に合わせて設定する。

例：

```ts
export default defineConfig({
  base: '/TIFFScheduler/',
});
```

---

## 15. MVP スコープ

MVP に含める。

- TIFF 作品一覧表示
- 見たい作品の複数選択
- 同一作品の複数上映回から最適上映回を選択
- 上映時間重複チェック
- 会場間移動時間の考慮
- 退場・入場バッファ
- 昼食時間 45 分の確保
- 通常勤務曜日設定
- 追加休日設定
- 休暇取得不可日設定
- 必要休暇日数の最小化
- 総移動時間の最小化
- 総待ち時間の最小化
- おすすめプラン最大 3 件表示
- 全作品を鑑賞できない場合の代替案表示
- スマートフォン対応
- GitHub Pages デプロイ

---

## 16. MVP 対象外

初期バージョンでは以下を対象外とする。

- ユーザーアカウント
- サーバーサイド API
- データベース
- Google Maps API 等のブラウザからのリアルタイム経路検索
- チケット購入機能
- TIFF アカウントとの連携
- 複数ユーザー間でのスケジュール共有
- Push 通知

---

## 17. 将来拡張候補

- TIFF 公式上映情報の自動取り込み
- 上映情報変更の自動検知
- PWA 対応
- スケジュールの URL 共有
- LocalStorage による選択状態保存
- Q&A / 舞台挨拶付き上映の優先設定
- 1 日最大鑑賞本数の指定
- 最遅帰宅時間の指定
- 朝早い上映を避ける設定
- 好みの会場を優先する設定
- 移動負荷を減らすモード
- 昼食時間帯・必要時間のユーザー設定
- 昼食場所候補の表示
- カレンダー形式の結果表示
- iCalendar (`.ics`) 出力

---

## 18. 非機能要件

### パフォーマンス

- 一般的な TIFF 鑑賞候補数に対し、ブラウザ上で実用的な時間内に最適化を完了する
- 探索中に UI を長時間ブロックする場合は Web Worker の利用を検討する

### モバイル対応

- iPhone Safari を含むモバイルブラウザで利用可能とする
- 320px 程度の画面幅でも横スクロールなしで主要操作を行えること

### 保守性

- TIFF 固有データとスケジューラロジックを分離する
- 制約判定とスコア計算を独立モジュールとする
- 将来の映画祭や上映イベントへの転用も可能な構造を意識する

### テスト

特に `scheduler` は UI から独立させ、ユニットテスト可能にする。

最低限、以下をテストする。

- 上映時間重複
- 同一会場の連続上映
- 異なる会場への移動
- 入退場バッファ
- 昼食制約
- 勤務日と休日の判定
- 休暇日数計算
- 複数上映回からの最適選択
- 全作品鑑賞不可時の最大鑑賞数
- 同一休暇日数での移動時間比較

---

## 19. 実装優先順位

### Phase 1: Scheduler Core

- 型定義
- 上映衝突判定
- 移動可能判定
- 休暇日数計算
- DFS + Branch and Bound
- ユニットテスト

### Phase 2: Basic UI

- 作品一覧
- 作品選択
- 条件設定
- スケジュール生成
- 日別タイムライン表示

### Phase 3: Real TIFF Data

- 2025 TIFF 上映データ投入（2026年データ未公開のためユーザー指定で変更）
- 会場間移動時間マトリクス作成
- データ検証

### Phase 4: Deployment

- GitHub Actions
- GitHub Pages
- モバイル動作確認

### Phase 5: Improvements

- 代替案の説明改善
- Web Worker
- PWA
- 自動データ取り込み

---

## 20. 未決事項

実装開始前または実データ投入時に決定する。

1. 平日の「休暇が必要」と判定する勤務時間帯
2. 半休を考慮するか
3. 昼食制約を適用する日の厳密な条件
4. 同一映画館・同一スクリーン付近での最低移動時間
5. Q&A / 舞台挨拶時間を必須滞在時間として扱うか
6. TIFF 公式サイトからのデータ取得方式
7. 祝日データの管理方法
8. 最適解が多数存在する場合の上位 3 件の多様性確保方法

---

## 21. MVP 完了条件

以下をすべて満たした時点で MVP 完了とする。

- 実際の TIFF 上映データを読み込める
- 複数作品を選択できる
- 有効な上映回の組み合わせを自動生成できる
- 映画館間移動時間を考慮できる
- 昼食時間を確保できる
- 必要休暇日数を計算し最小化できる
- 結果を日別タイムラインとして確認できる
- 全作品を鑑賞できない場合に代替案を提示できる
- iPhone Safari で主要機能を操作できる
- GitHub Pages で公開できる
